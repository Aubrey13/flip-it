using UnityEngine;
using System.Collections;

public class Bool : MonoBehaviour {
	/*
		if(isFlying == false) {
			Debug.Log("isFlying is now " + isFlying); //get the return value from FlyingTest and set it to isFlying isFlying = FlyingTest(); Debug.Log("isFlying is now " + isFlying);
		} else if(isFlying == true) {
			Debug.Log("isFlying is already true");
		}
	}
	//You can put bool as a return type and as a parameter type bool FlyingTest() {
	return true;
}*/
}