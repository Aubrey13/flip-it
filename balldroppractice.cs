using UnityEngine;
using System.Collections;

public class NewBehaviourScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
		public float balldrop = 45;
		public float balldroplimit = 50.0;
		public float balldropnobounce = 0.0;
		
		void Update ()
		{
			if(Input.GetKeyDown(KeyCode.Space))
				balldroptest();
			void ballDropTest()
			{
				if(balldrop > balldroplimit);
				{
					Debug.Log(“Ball will pop”)
				}
				else if(balldrop<balldropnobounce);
				{
					Debug.Log(“Ball won’t bounce”)
					{
						else
						{
							Debug.Log(Ball will bounce)
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
