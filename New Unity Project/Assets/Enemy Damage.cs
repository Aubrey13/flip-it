using UnityEngine;
using System.Collections;

public class EnemyDamage : MonoBehaviour {

	// Use this for initialization // what are parameters? how do we list them? what is the tag? what does it look like in scripting? 
	void Start () {
		// void OnCollisionEnter(enemeyCollision)
		// If(*collisionVariable*.transform.tag == "type the tag name here")  
				//Do stuff when this hits an GameObject with the tag specified above. Replace *collisionVariable* with the name that you have given the Collision variable.
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
